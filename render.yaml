services:
  - type: web
    name: exhibit-merge-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: RENDER_EXTERNAL_HOSTNAME
        value: YOUR_RENDER_SUBDOMAIN.onrender.com